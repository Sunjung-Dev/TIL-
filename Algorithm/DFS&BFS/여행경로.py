from collections import deque

def bfs(tickets, answer):
    queue = deque()

    for i in range(len(tickets)):
        if i == 0:
            queue.append(tickets[0][1])
        for i in range(i)
        

    return 


def solution(tickets):
    answer = []
    bfs(tickets, answer)
    return answer